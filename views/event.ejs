<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dunedin Euchre – <%= eventTitle %></title>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="manifest" href="/manifest.json">
  <meta name="theme-color" content="#0081A7">
</head>
<body>
<header class="site-header">
  <div class="header-inner">
    <img src="/images/dunedin-logo-transparent.png" alt="Dunedin Euchre" class="site-logo">
    <span class="site-title">Dunedin Euchre</span>
    <span class="header-sub">Monthly Tournament</span>
  </div>
</header>

<main class="event-page-main">
  <%- include('./partials/event-overview', {
    event,
    formatEventDate,
    formatTime,
    eventTitle,
    arrivalNotes
  }) %>

  <section class="card roster-card" style="margin-top:1.5rem;">
    <div class="public-page-header">
      <div>
        <p class="event-kicker" style="margin-bottom:0.35rem;">Invitee summary</p>
        <h3 class="roster-heading" style="margin-bottom:0;">RSVP summary</h3>
      </div>
      <p class="public-summary-copy"><strong><%= summary.responded %></strong> of <strong><%= summary.invited %></strong> invited players have responded.</p>
    </div>

    <div class="roster-counts">
      <span class="count-badge count-yes"><%= summary.yes %> Yes</span>
      <span class="count-badge count-maybe"><%= summary.maybe %> Maybe</span>
      <span class="count-badge count-no"><%= summary.no %> No</span>
      <span class="count-badge count-none"><%= summary.pending %> Pending</span>
    </div>

    <div class="public-roster-grid">
      <section class="public-roster-section">
        <h4 class="public-roster-heading">Coming</h4>
        <% if (groupedRoster.yes.length > 0) { %>
          <ul class="public-roster-list">
            <% groupedRoster.yes.forEach(function(entry) { %>
              <li class="public-roster-item"><%= entry.name %></li>
            <% }); %>
          </ul>
        <% } else { %>
          <p class="public-roster-empty">No confirmations yet.</p>
        <% } %>
      </section>

      <section class="public-roster-section">
        <h4 class="public-roster-heading">Maybe</h4>
        <% if (groupedRoster.maybe.length > 0) { %>
          <ul class="public-roster-list">
            <% groupedRoster.maybe.forEach(function(entry) { %>
              <li class="public-roster-item"><%= entry.name %></li>
            <% }); %>
          </ul>
        <% } else { %>
          <p class="public-roster-empty">No maybes yet.</p>
        <% } %>
      </section>

      <section class="public-roster-section">
        <h4 class="public-roster-heading">Can't make it</h4>
        <% if (groupedRoster.no.length > 0) { %>
          <ul class="public-roster-list">
            <% groupedRoster.no.forEach(function(entry) { %>
              <li class="public-roster-item"><%= entry.name %></li>
            <% }); %>
          </ul>
        <% } else { %>
          <p class="public-roster-empty">No declines yet.</p>
        <% } %>
      </section>
    </div>
  </section>
</main>
</body>
</html>
